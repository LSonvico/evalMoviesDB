<!DOCTYPE html>
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<html lang="es">
<body>
  <div>
	  <h2>Detalle de pelicula</h2>
	</div>				
		<div>
			<section>
				<article>
				<p><strong>Titulo</strong>: <%= pelicula.title %> </p>
				<p><strong>Rating</strong>: <%= pelicula.rating %> </p>
				<p><strong>Premios</strong>: <%= pelicula.awards %> </p>
				<p><strong>Duracion</strong>: <%= pelicula.length %> </p>
				<p><strong>Fecha de Estreno</strong>: <%= pelicula.release_date %> </p>
				</article>
				<label for="">Actores</label>
				<br>
				<select name="actors" multiple>
				  <% actores.forEach(actor => { %>
					<% if (pelicula.actores.some(actorPelicula => actorPelicula.id == actor.id)) { %>
					  <option value="<%= actor.id %>" selected> <%= actor.first_name %> <%= actor.last_name %></option>
					  <% } else { %>
						<!-- <option value="<%= actor.id %>"> <%= actor.first_name %> <%= actor.last_name %></option> this line is used to show all actors that are in the DB, not commeting it will make all the DB actors appearing -->
						<% } %> 
					<% }) %>
				</select>
			</section>
			<form action='/movies/edit/<%=pelicula.id%>' method='GET'><button class= "modificar" type='submit' >Modificar</button></form>
			<form action='/movies/delete/<%=pelicula.id%>?_method=DELETE' method='POST'><button type='submit'>Borrar</button></form>
		</div>
</body>	
</html>